'use strict';

// Keep the Next.js runtime on the canonical server implementation so security,
// eligibility, panorama matching, and quota controls cannot drift.
module.exports = require('../../../server/routes/property-image');
