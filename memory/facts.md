# Memory — Facts (invariants, decisions, ground truth)

> Persistent global facts. Every entry cites its source file. No hallucinated facts.
> Updated by the agent after significant changes; read at session start.
>
> Last refreshed: 2026-09-14 (forms persistence + completion audit).

## Architecture

- **Three layers**: v0 static PWA (`index.html`/`app.js`), v1 Node `http` API (`server/`), v2 Next.js 16 App Router (`src/`).
  - Source: `CONTEXT.md`, `AGENTS.md`
- **Terminal filter/view store**: `src/lib/terminal-filter-store.ts` is the pure shared source of truth for live-feed filters, sort, and grid/map/parser view. `interactive-terminal.tsx` uses `useReducer` + `selectFilteredListings`. Keep filter logic out of the component. Store must stay free of `@/`/`@server` imports so Node tests can load it.
  - Source: `src/lib/terminal-filter-store.ts`, `src/components/terminal/interactive-terminal.tsx`
- **Hunt match ranking**: `server/intelligence/hunt-ranking.js` ranks *matched* hunt results for triage (criterion closeness, research quality, urgency, SCORE_BANDS band). Binary match remains the gate; rank is presentation-only and never invents publisher facts. `server/intelligence/score-bands.js` + `src/lib/score-bands.ts` are the Elite/Strong/Fair/Thin presentation bands.
  - Source: `server/intelligence/hunt-ranking.js`, `server/intelligence/hunts.js`, `src/lib/score-bands.ts`, `test/hunt-ranking.test.js`
- **No database required to boot**: `server/db/client.js` falls back to in-memory provider seeded from `data.js` when `DATABASE_URL` is unset.
  - Source: `server/db/client.js`, `AGENTS.md`
- **API port**: `server/server.js` listens on port 3000; Next dev on 3001. Base44 compose maps UI to 3000 and proxies API.
  - Source: `AGENTS.md`, `docker-compose.base44.yml`

## Invariants (do not violate)

- Every dynamic/AI/user string renders through `esc()` / `mdToHtml()` (XSS invariant).
  - Source: `CONTEXT.md`, `app.js`
- `SCORE_BANDS` is the single source of truth for Deal Score color/label/alpha.
  - Source: `CONTEXT.md`, `app.js`
- Listing contract is camelCase (`dealScore`, `openingBid`, `propType`); Postgres stores snake_case and `server/db/client.js` aliases back.
  - Source: `CONTEXT.md`, `server/db/client.js`
- All scraper network calls must be wrapped in `ScraperCircuitBreaker`.
  - Source: `.agents/skills/property-scraper-engineering/SKILL.md`, `server/scrapers/circuit-breaker.js`
- Never overwrite existing records with zero-byte or 403 error payloads.
  - Source: `.agents/skills/property-scraper-engineering/SKILL.md`
- Document review state machine: rejected/needs_more require a note; approved/rejected/needs_more require a reviewer identifier; status changes increment revision; same-status re-confirmation preserves `reviewedAt` + `priorStatus`. All returned objects are frozen.
  - Source: `server/intelligence/document-review.js`
- Document id format: `${listingId}~${documentIndex}~${documentUrl}` with `~` as the URL-safe separator (avoids `#` URL fragment collisions).
  - Source: `server/routes/document-review.js`

## Data

- 16 source types (A/B tier): bid4assets, civilview, fannie, fdic, freddie, gsa, hud, irs, landbank, marshals, servicelink, sheriff, treasury, trustee, usda, va.
  - Source: `CONTEXT.md`, `data.js`
- 2096 listings across 51 states (states includes all 50 US states + DC territory + PR).
  - Property types: Commercial, Condo, Duplex, Land, Single Family, Single Family Home, Town House, Triplex, Unknown.
  - Source: `CONTEXT.md` (regenerated 2026-09-14), `data.js`
- Server API route modules: alerts, auction-calendar, coverage, document-review, enrich, enrichment, export, hunts, listings, neighborhoods, parcel-boundary, parse, property-image, property-image-providers, property-intelligence, property-signals, saved-searches, scrapers, source-network, verify-docket, watchlist-comps, workspace.
  - Source: `CONTEXT.md`, `server/routes/`
  - Source: `CONTEXT.md`, `server/routes/`
- 35 scraper modules in `server/scrapers/` (adapter + infrastructure mix).
  - Source: `server/scrapers/`

## Agent system

- Commands live in `.kilo/command/*.md` — 5 commands: scrape, refresh-data, test, review, research.
  - Source: `.kilo/command/`
- Agents live in `.kilo/agent/*.md` — 10 agents: scraper-engineer, reviewer, qa-engineer, coordinator, developer, researcher, analyzer, documenter, monitor, specialist.
  - Source: `.kilo/agent/`
- Skill index at `.agents/skills-index.json` — generated by `scripts/gen-skills-index.js`.
  - Source: `scripts/gen-skills-index.js`
- Skill router at `scripts/skill-router.js` — ranked disambiguation, top-3 candidates.
  - Source: `scripts/skill-router.js`
- Proportional completion gate at `scripts/verify-gate.js` — classifies change, runs proportionate suite.
  - Source: `scripts/verify-gate.js`
- CONTEXT.md is auto-generated by `scripts/gen-context.js` — drift-gated by `test/context.test.js`.
  - Source: `scripts/gen-context.js`, `test/context.test.js`
- Skills doctor at `scripts/skills-doctor.js` — flags duplicates, empty dirs, epoch files, stale bundles.
  - Source: `scripts/skills-doctor.js`, `test/skills-doctor.test.js`
- Capability Graph at `scripts/capability-graph.js` — typed DAG orchestrator with input/output validation, cycle detection, topological sort, and stratified parallel stage execution.
  - Source: `scripts/capability-graph.js`, `test/capability-graph.test.js`
- Opportunity Signal Evaluator at `server/intelligence/signals.js` — 6 deterministic candidate signals with tri-state status, published component weights, and triage priority.
  - Source: `server/intelligence/signals.js`, `test/signals.test.js`
- Database Seeder at `scripts/seed-from-v0.js` — parameter-safe PostgreSQL seeder from v0 `data.js` with dry-run validation.
  - Source: `scripts/seed-from-v0.js`, `test/seed-from-v0.test.js`
- Swarm orchestrator at `scripts/swarm/` + `scripts/swarm.js` CLI — 7 strategies, 9 agent types, 5 coordination modes, work-stealing queue with lease fencing, capability-graph integration, dry-run, 22 tests.
  - Source: `scripts/swarm/orchestrator.js`, `test/swarm.test.js`

## Competitive intelligence (2026-09-13)

- Source status taxonomy: 7 statuses (`VERIFIED_OFFICIAL`, `VERIFIED_FIRST_PARTY`, `SCOPE_LIMITED`, `LOCAL_ROUTE`, `DISCOVERY_ONLY`, `INCONCLUSIVE_BLOCKED`, `RETIRED`) mapped to all 58 catalog entries.
  - Source: `server/sources/catalog.js` `SOURCE_STATUSES`, `SOURCE_STATUS_BY_ID`
- Every listing API response includes `triage` object: `{ isNew, priceDropped, staleDays, hasDocs, occupancyKnown, distressStage }`.
  - Source: `server/scrapers/normalization.js` `computeTriage()`, `server/routes/listings.js` `presentListing`
- `parcelKey` = `${countyFips}-${normalizedApn}` when FIPS+APN available; `null` otherwise. Never fabricated.
  - Source: `server/scrapers/normalization.js` `buildParcelKey()`, `normalizeApn()`
- 9 new discovery-only catalog sources: fl-dor-cadastral, tx-cad-bulk, county-recorder-nod, courtlistener, ca-controller-tax-sale, fhfa-hpi, census-geocoder, mers-servicerid, excess-funds.
  - Source: `server/sources/catalog.js`
- Hardcoded api.market API key removed from `zillow-mcp.js` and `property-title.js`; both throw if env unset. Tracked markdown previously contained the raw key — **redacted**; operator must rotate the historical api.market credential offline.
  - Source: `server/intelligence/zillow-mcp.js`, `server/intelligence/property-title.js`
- Puter.js fully removed from `src/`; zero `puter` matches. AI paths use backend enrich endpoint.
  - Source: `src/app/layout.tsx`, `src/components/terminal/property-drawer.tsx`
- Contact/newsletter forms POST to `/api/contact` and `/api/newsletter`; both persist to `.cache/form-submissions/*.jsonl` via `src/lib/form-submissions.js` and report `delivery: local|forwarded`. UI copy does not claim email delivery when no webhook is set.
  - Source: `src/lib/form-submissions.js`, `src/app/api/contact/route.ts`, `src/app/api/newsletter/route.ts`, `test/form-submissions.test.js`
- verify.js expanded to 36 suites including CONTEXT drift, discovery backend/ops, property documents, walkthrough providers, swarm orchestrator, form persistence.
  - Source: `test/verify.js`
- verify-gate.js full-gate timeout raised from 120s to 30 min.
  - Source: `scripts/verify-gate.js:117`
- Delta-sync `?since=<ISO>` on `/api/listings`: filters by `sourceObservedAt`/`provenance.observedAt`/`fetchedAt`, returns `delta:true` + `since` echo, 400 on invalid date.
  - Source: `server/routes/listings.js` `listingTimestampMs()`
- Source Radar renders 7-status taxonomy badges with summary line ("X verified · Y scope-limited · Z discovery-only · W blocked").
  - Source: `src/components/sources/source-network.tsx` `SOURCE_STATUSES`, `taxonomyBadge()`, `taxonomySummary()`
- Discovery Workbench has 5 triage filter chips (New / Price dropped / Has docs / Stale >30d / Occupancy known) with AND logic; cards show New/Price cut/Docs/Stale/REO badges.
  - Source: `src/components/listings/discovery-workbench.tsx`, `src/components/listings/discovery-card.tsx`
- Distress lifecycle filter (All/REO/Pre-FC/Scheduled/Tax sale/Unknown) in Discovery Workbench; color-coded card badges.
  - Source: `src/components/listings/discovery-workbench.tsx`, `src/lib/discovery-query.ts`
- Multi-source bake-off: `computeBakeOff` + `applyCrossSourceBakeOff` annotate listings with `crossSourceMatches[]` and `bakeOff{preferredSource, reason, confidence}` on parcelKey collisions.
  - Source: `server/db/client.js`
- Scrapling bridge hardened: SSRF private-IP blocking, path traversal guards, script validation, 4MB Python input guard, `table-extract` profile, mailto/tel filtering. Tests 14→31 JS, 2→10 Python.
  - Source: `server/scrapers/scrapling-bridge.js`, `scripts/crawlers/scrapling_extract.py`
- Unbrowse/onboarding-spider hardened: SSRF IPv4-mapped IPv6 fix, content-hash validation, Accept headers, multi-path package-root detection. Tests 15→41.
  - Source: `scripts/crawler-tools-unbrowse.cjs`, `server/crawlers/onboarding-spider.js`
- FL DOR statewide cadastral adapter: ArcGIS FeatureServer at `services9.arcgis.com/Gh9awoU677aKree0/.../Florida_Statewide_Cadastral/FeatureServer/0`, 67-county FIPS mapping, pagination, circuit breaker. Status DISCOVERY_ONLY pending two clean canaries.
  - Source: `server/scrapers/fl-dor-cadastral.js`, `test/fl-dor-cadastral.test.js`
- CourtListener/RECAP adapter: v4 REST API search, cursor pagination, 1req/sec rate limit, `COURTLISTENER_API_KEY` auth, enrichment-only (never fabricates bid/sale). 24/24 tests.
  - Source: `server/scrapers/courtlistener.js`, `test/courtlistener.test.js`
- CA Controller tax-sale adapter: directory + county parcel parsing, 50-record budget, Scrapling `table-extract` optional, Cloudflare fail-closed. 27/27 tests.
  - Source: `server/scrapers/ca-controller-tax-sale.js`, `test/ca-controller-tax-sale.test.js`
- Per-county coverage matrix in Source Radar: 6 dimensions (assessor/recorder/foreclosure/taxSale/parcelMap/documentImages) × 10 seeded sources, toggle button, "Not mapped" for unmapped.
  - Source: `src/components/sources/source-network.tsx` `COVERAGE_PRESETS`
- Swarm `canary` strategy (8th): auto-routes on canary/promote/rollout/source-gate, phases plan→execute→gate→report, maxAgents 6.
  - Source: `scripts/swarm/config.js`, `scripts/swarm/orchestrator.js`
- Live canary results (demo mode, no PG): FL DOR PASS (20/20 parcelKey, 36 fetched/16 rejected), CourtListener PASS (10/10 raw+docket, 8 states), CA Controller FAIL CLOSED (Cloudflare 403, circuit breaker trips correctly).
  - Source: `reports/live-canary-results-2026-09-13.md`

## Document content review (2026-09-14)

- **Pure state machine** at `server/intelligence/document-review.js` — 4 states (`pending`, `approved`, `rejected`, `needs_more`). Persistence-free; caller owns storage.
  - Exports: `REVIEW_STATES`, `validateReviewInput(input)`, `applyReview(current, proposed, options)`, `normalizeReview(record)`, `createPendingReview(options)`, `filterByStatus(reviews, status)`, `summarizeReviews(reviews)`.
  - Note: `cleanNote` trims + whitespace-collapses + caps at 2000 chars; `cleanReviewer` trims + case-preserves + caps at 200 chars.
  - Source: `server/intelligence/document-review.js`, `test/document-review.test.js` (32 tests)
- **HTTP route** at `server/routes/document-review.js` — thin wrapper over the pure module. In-memory `Map` storage (placeholder until Postgres persistence is designed).
  - Endpoints: `GET /api/document-review` (list, optional `?status=` filter), `POST /api/document-review` (apply review), `GET /api/document-review/{id}` (single).
  - Error codes: 400 on missing `listingId` / invalid JSON; 422 with stable error codes from the pure module on state-machine violations; 404 on missing id; 405 on unsupported methods.
  - Body cap: 32 KB; listingId cap: 200 chars.
  - Exports `_resetForTests` + `_store` for unit tests.
  - Source: `server/routes/document-review.js`, `test/document-review-route.test.js` (15 tests)
- **Wired into `server/server.js`** as the 15th API route module (`handleDocumentReview`).
  - Source: `server/server.js` (line 19), commit `cfa53b3`
- Closes the deferred "document content review" item from `docs/source-live-audit.md`. Builds on the existing build-data extraction classifier in `server/intelligence/document-evidence.js` (observed / unknown / none_observed).
- Trust-status taxonomy also surfaced: `trustStatus` exposed alongside runtime status in `server/sources/network.js` (`buildSourceNetwork`); `triage` + `parcelKey` wired at `server/scrapers/normalization.js`, `server/db/client.js:202-209`, `server/routes/listings.js:36-39`.
  - Source: `.dsh-project-memory/tasks.json` (tsk_d962d462_production-hardening-and_1788941257669, step 3)

## Repo metrics (2026-09-14)

- Total commits on `main`: 95 (`git rev-list --count HEAD`).
- Test files in `test/`: 121 (`*.test.js`, `*.test.mjs`).
- Suites in `test/verify.js`: 35 (npm test entry).
- `CONTEXT.md` regenerated 2026-09-14 (digest `511f2916ce84...`); `node scripts/gen-context.js --check` passes.
- `package.json` `test:source-integrity` script now also runs `test/discovery-workbench-chips.test.mjs`.
- API route modules: 15.
- Scraper modules: 35.
- Intelligence modules: 10 (`document-evidence`, `document-review`, `dossier`, `hunt-store`, `hunts`, `property-title`, `research-cases`, `research-store`, `signals`, `zillow-mcp`).
- Production-hardening task `tsk_d962d462_production-hardening-and_1788941257669` archived 2026-09-14 with all 5 steps complete; deferred items remain in `memory/facts.md` and `reports/property-experience-2026-09-12.md`.
## Scrapling + scraper expansion (2026-09-14)

- **Scrapling protocol was broken for HUD/Treasury/IRS/page-links** until this session: `validResponse` only validated gsa-index/gsa-detail/table-extract, so real Python extractions of the other profiles always returned `SCRAPLING_PROTOCOL_ERROR`. Fixed with per-profile validators; Python `page-links` emits only HTTPS; IPv4-mapped IPv6 SSRF (`::ffff:127.0.0.1`) is rejected.
  - Source: `server/scrapers/scrapling-bridge.js`, `scripts/crawlers/scrapling_extract.py`
- Scrapling profiles now: gsa-index, gsa-detail, page-links, table-extract, hud-cards, treasury-detail, irs-detail, usda-table, civilview-sales. Doctor ready with scrapling==0.4.15.
  - Source: `scripts/scrapling-doctor.js`, `scripts/crawlers/scrapling_extract.py`
- Fannie/Freddie/VA/Marshals/Sheriff emit `lastRunReport` and throw `*_UPSTREAM_UNAVAILABLE` when every unit fails (no more silent `[]`).
  - Source: `server/scrapers/run-report.js`, fannie/freddie/va/marshals/sheriff.js
- Sheriff default counties expanded to 10 OH Realauction domains; extras via `SHERIFF_EXTRA_COUNTIES`. VA base via `VA_REO_BASE_URL`.
  - Source: `server/scrapers/sheriff.js`, `va.js`, `.env.example`
- Tests: scrapling+scraper-upgrade 69/69, crawler-tools+civilview 45/45, python parser 16/16.

## Live canary script (2026-09-14)

- `scripts/canary-live.js` wraps discovery-worker Migration 014 canaries: `list`, `status`, `run --all-promoted|--sources|--wave --repeat N --scrapling`, `promote --source`. Dry-runs without DATABASE_URL; writes `.cache/canary-reports/*.json`.
  - Source: `scripts/canary-live.js`, `test/canary-live.test.js`, npm scripts `canary:live|list|status|promote`

## Self-audit canary/scrapling (2026-09-14)

- evaluateCanaryClean rejects non-zero rejected/recordsRejected and fixtureFallbackUsed (Migration 014 rejected_count=0).
- canary-live strict exit + distinct runId tracking + refreshScraplingFlags after env set.
- scrapling-bridge treasury/irs detail requires typed fields; refreshScraplingFlags exported.
- usda.js + civilview.js call Scrapling (usda-table / civilview-sales) when enabled.
- Remaining: fannie/freddie/va/marshals/sheriff declare useScrapling but do not call extract; SPA 200+empty still classifies as empty.
- Source: scripts/canary-live.js, scrapling-bridge.js, usda.js, civilview.js


## Proceed follow-ups (2026-09-14)

- SPA shells (Fannie/Freddie/VA) now observation_error via looksLikeClientRenderedShell + applyEmptyInventoryHonesty; fixtures stripped (~1.4k lines demo inventory removed).
- source-network-http collector count uses catalog invariants not hardcoded 17.
- newsletter UI test accepts honest local-delivery status.
- npm test: discovery 3i/3j fail without DISCOVERY_TEST_DATABASE_URL (PostgreSQL); Playwright suite has many pre-existing 5s timeouts on this host.
- Pushed origin/main: c91ea2a, bc4159b, 72461f2.


## Scraper power guarantee (2026-09-18)

- docs/SCRAPER_POWER_GUARANTEE.md + reports/scraper-power-guarantee.json generated by scripts/scraper-power-report.js (npm run scrapers:power).
- 59 catalog sources, 21 scheduled adapters, 2096 seed listings, 10/10 required ends scheduled.
- New enrollment templates: government-land + local-surplus (skipped_not_enrolled until GOV_LAND_DISCOVERY_URL / LOCAL_SURPLUS_DISCOVERY_URL).
- Code guarantee ≠ live volume guarantee; CAPTCHA/SPA/WAF sources still fail closed.


## Closed undone tasks (2026-09-18)

- T2-T7 closed: PRODUCT_GAPS.md, quality:report 10/10 non-PG pass, smoke:production OK, IMAP complete, swarm real mode tests, promotion-gate contract without PostgreSQL.
- npm scripts: swarm:real, quality:report, smoke:production, promotion:gate, scrapers:power.


## Foolproof scrape research (2026-09-18)

- docs/FOOLPROOF_SCRAPE_RESEARCH.md: HN/GitHub/Scrapling findings — adaptive parse, capture_xhr for Fannie/Freddie/VA SPA, schema-first LLM extract, AutoThrottle, waterfall fetch; CAPTCHA bypass rejected for .gov ethics.
- server/scrapers/fetch-strategy.js + test/fetch-strategy.test.js (npm run test:fetch-strategy).
- Sources: HN Algolia Draco/Runo/Spidra/Crawlee/State-of-Scraping-2025; GitHub D4Vinci/Scrapling, apify/crawlee. Reddit JSON 403; websearch disabled.


## Foolproof scrape P0 (2026-09-18)

- spa-xhr.js + scrapling_spa_xhr.py capture_xhr lane for fannie/freddie/va (SCRAPLING_SPA_XHR=1).
- auto-throttle.js + Retry-After in http.js fetchTextWithPolicy.
- listing-schema.js identity gate in BaseScraper.standardizeListing.
- Tests 50/50 foolproof-p0 + scrapers + server; pushed to origin/main.


## Security + durability re-audit (2026-09-19/20 strict)

- Tracked api.market key redacted from `reports/swarm-analysis-2026-09-13.md` and `memory/facts.md`; rotate historical credential offline.
- Anonymous `GET /api/property-intelligence` no longer returns stored research/public-record extensions (`researchRestricted:true`); operator Bearer required.
- Document-review: durable file store (`server/intelligence/document-review-store.js`), listing-document hydration for pending queue, transition table + optional revision check, percent-encoded document ids, POST body reuse of `req.body` (fixes deadlock after `server.js parseJsonBody`), persist failure → 503.
- Next proxy: `API_PATH` covers unbrowse/jobs/import; App Router routes for jobs list, unbrowse status/intake, workspace import preview/commit; `test/property-api-proxy-inventory.test.js` in quality gate.
- CORS: API and Next no longer trust request Host / naive origin equality; loopback+0.0.0.0 normalized; mutations require Origin or `x-workspace-request: 1`.
- SSRF: `scraplingHttpGet` rejects private/loopback/metadata hosts + non-HTTPS + credentials.
- `tokensMatch` hashes both sides before timingSafeEqual; client `operatorToken` prop removed from enrichment view.
- Fly/Koyeb configs document required `SCRAPER_ADMIN_TOKEN`; Fly `memory_mb` only; Base44 compose api loads `.env.local`.
- AGENTS.md seed truth updated to 2096 listings / 16 sources.
- Production boot: `scripts/production-env.js` loads `.env.local`; internal API port `publicPort+2` for non-3000.
- Verified: quality gate **13/13** (PG set); tsc clean; live e2e **23/23** on `:3970`/`:3972` (unlock, review write+persist, durable readback, unbrowse, jobs, import preview).
## Strict improvement pass (2026-09-20 follow-up)

- **Durability honesty:** document-review is a **host-local file store** (process-restart durable, not redeploy-durable). UI + PRODUCT_GAPS say so; `/api/health` returns `dataMode` (demo|postgres) + `documentReviewStore` (file|none) + path.
- **Volumes:** `docker-compose.yml` `appcache:/app/.cache`; `fly.toml` `[[mounts]] property_cache → /app/.cache`; Koyeb env paths + disk docs. Base44 compose **no longer requires** `.env.local` via `env_file` (Node `--env-file-if-exists` only).
- **Store lock:** document-review persist uses exclusive `.lock` (live-record-store protocol); failed persist **rolls back** in-memory map so GET cannot lie; HTTP illegal transition → 422.
- **e2e runner:** isolated `PROPERTY_DOCUMENT_REVIEW_STORE_PATH` under tmpdir; 180s timeout; awaits taskkill; waits for ports free; health logs dataMode.
- **CSP:** Next script-src without `unsafe-eval`; map markers use `textContent` not interpolated innerHTML; gtm team.desc is text nodes.
- **CSRF:** `workspaceMutationAllowed` does not trust `X-Forwarded-Host` unless `TRUSTED_PROXY_COUNT>0`; accepts `PUBLIC_APP_ORIGIN`; tests in `test/workspace-mutation-gate.test.js`.
- **CI:** unit-gate timeout 40m; e2e after build; verify.js suite 10 includes `production-boot.test.js`.
- **start-production:** refuses boot when `.next/BUILD_ID` missing.
- **Verified:** quality **13/13** (PG), production e2e **23/23** (`:3980`, isolated store, `dataMode=demo documentReviewStore=file`).
## Missed-gap closure (2026-09-20 follow-up 2)

- **AGENTS.md**: seed truth 2096/16; verify comments no longer claim “50 listings only”; documents optional `SCRAPER_ADMIN_TOKEN` and `/api/health` honesty fields.
- **UI demo banner**: `src/components/site/data-mode-banner.tsx` on `/listings` + `/workspace/documents-review`; reads `/api/health` `dataMode`/`documentReviewStore`.
- **Postgres document_reviews**: `server/db/schema.sql` + `document-review-store.js` pool backend (`persistAsync` / `loadPg`); route binds pool from `db` on first request; health reports `postgres|file|none`.
- **Intelligence auth tests**: wrong token, POST denied, token-unset restricted path.
- **E2E**: asserts health honesty fields; **24/24** on live stack with `dataMode=postgres documentReviewStore=postgres` (review write+readback through PG path).
- **Quality**: 13/13 with PG; CONTEXT current; tsc clean.
- **Source Radar (2026-09-20):** approved Migration 014 gates show **Collection approved** + scope hint; pending remains Approval pending/blocked. Publisher catalog taxonomy is no longer the only status chip.
- **Dockerfile.production** explicitly copies `server/db/schema.sql` (document_reviews). Production smoke asserts schema + store + proxy contract.
- **Schema mirrors (2026-09-20):** `src/lib/db/schema.sql` byte-synced with `server/db/schema.sql` after `document_reviews`; `test/db.test.js` in unit-gate.
- **CSP:** `style-src` allows `fonts.googleapis.com`; `font-src` allows `fonts.gstatic.com`. `script-src 'unsafe-inline'` remains a documented Next bootstrap residual.
- **HUD catalog:** `hud-homestore` status **SCOPE_LIMITED**; UI coverage preset “OH, NJ promoted run scope — not nationwide”.
- **E2E default pins demo** (`delete DATABASE_URL`); `npm run test:production-e2e:db` opts into PG path.
- **Explore-5 closure (2026-09-20):** schema mirror synced; quality-gate `forms+boot` includes queue-ui/hardening/source-catalog; isolated `db-contract` suite strips ambient DATABASE_URL; inventory tests cover template-literal prefixes; e2e default **demo-pinned** (25/25) + `--with-db` PG path; HUD catalog SCOPE_LIMITED; CSP fonts allowed; hardening fixtures isolated from live PG.
- **Ultraplan execution (2026-09-20 begin):** Phase 0 commits f94e113/300bdb7/d29e1de/ac804e6; Phase 1 CI 814611e (schema:check, demo-pin e2e, optional PG job). Phase 3: **servicelink promoted** after 2 CLEAN canaries (maxPages=100); fixed sweepStartedAt bug. Phase 2: CSP nonce blocked on static pages per Next 16 docs — residual documented.
- **Still open:** Next script-src unsafe-inline; gsa/irs/wave2 canaries; operator secrets/volume/Maps key; optional live-PG db.test quirks.
- **HUD promotion (2026-09-20):** `hud` **promoted** after 2 clean canaries at declared scope `HUD_STATES=OH,NJ`, `HUD_MAX_PAGES_PER_STATE=20`, `HUD_PAGE_SIZE=50`. Scope hash `da1ae8f3080f…`. maxPages=2 first attempt was NOT_CLEAN (gate held). Report: `reports/canary-promotion-hud-2026-09-20.md`. Nationwide 52-state HUD remains unpromoted.

## Ultraplan (2026-09-20)

- Full remaining-work plan: `docs/ULTRAPLAN.md` — phases 0–6 (commit/land → CI truth → CSP → scoped canaries → PG db contract → operator pack → polish). Timebox order in §6.

