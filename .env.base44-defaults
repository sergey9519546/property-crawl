# Repo-level placeholder defaults so the app boots without credentials.
# Real secrets (delivered by the platform to /run/base44/app.env) always win
# because that file is listed AFTER this one in compose env_file ordering.
NODE_ENV=development
PORT=3000
PROPERTY_API_URL=http://localhost:3000
